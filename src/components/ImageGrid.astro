---
import { Image } from 'astro:assets';

interface Props {
  images: [ImageMetadata, string][];
}

const { images } = Astro.props;
---

<div class='grid grid-cols-2 sm:grid-cols-4 gap-0.5 not-prose'>
  {images.map(([src, alt], index) => <Image src={src} alt={alt} />)}
</div>
