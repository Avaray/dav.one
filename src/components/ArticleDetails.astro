---
import { readableDate } from '../scripts/utils.ts';
const articleUrl = new URL(Astro.request.url).toString();
const { title, created, updated, author } = Astro.props;
---

<div id='article-details' class='mb-8'>
  <h1>{title}</h1>
  <div class='flex items-center gap-2 flex-wrap'>
    <span class='rounded-[var(--radius-box)] border-solid border p-2 px-4 whitespace-normal md:whitespace-nowrap'>
      <span>Created on {readableDate(created, true)}</span>
      {author && <span> by {author}</span>}
    </span>
    {
      updated && (
        <span class='rounded-[var(--radius-box)] border-solid border p-2 px-4 whitespace-normal md:whitespace-nowrap'>
          <span>Updated on {readableDate(updated)}</span>
        </span>
      )
    }
    <span class='hidden print:block rounded-[var(--radius-box)] border-solid border p-2 px-4 w-fit'>
      {articleUrl}
    </span>
  </div>
</div>
